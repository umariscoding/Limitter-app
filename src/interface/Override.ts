export interface Override {
  id: string;                     
  user_id: string;
  device_id: string;
  limit_id: string;               
  used_at: number;                
  remaining_overrides: number;    
}